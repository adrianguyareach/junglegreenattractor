// Package event provides a typed publish-subscribe event system used by the
// pipeline engine to signal lifecycle transitions (stage started, completed,
// failed, retrying) to CLI output and external observers.
package event
