// Package handler implements all built-in node handlers for the pipeline
// engine. Each handler corresponds to a DOT node type (codergen, wait.human,
// tool, parallel, etc.) and is resolved at runtime through the Registry.
package handler
